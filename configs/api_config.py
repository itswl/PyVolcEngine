# API配置
api_config = {
    "ak": "AKxxxx",
    "sk": "WVxxx",
    "region": "cn-shanghai"
}

# 超时配置
timeout_config = {
    "instance_ready": 1800,  # 实例就绪超时时间（秒）
    "check_interval": 30     # 检查间隔（秒）
}
